# GEO 校验报告（迁移后）

日期：2026-09-22　校验对象：`npm run build` 产物 `dist/`（25 页）
校验方式：`geo-llmstxt`、`geo-schema`、`geo-citability` 三个技能的评分规则 + 本地产物脚本核对

---

## 一、llms.txt（geo-llmstxt）

产物：`dist/llms.txt`（62 行），数据来源为后台「站点设置」的 llms 分组 + 课程/知识百科/问答/文章数据。

| 检查项 | 结果 |
| --- | --- |
| H1 标题 | 通过（`# 美迪时代教育（MedE Education）`） |
| 简介 blockquote 长度 | 通过（197 字符 < 200；为达标已精简 llmsIntro，去掉「品牌使命」口号句） |
| H2 分节 | 通过（7 节：品牌事实 / 课程体系 / 知识百科 / 栏目导航 / 常见问题 / 联系方式 / 引用规范） |
| Key Facts 小节 | 通过（本次由普通加粗行升级为 `## 品牌事实（Key Facts）`，AI 更易定位） |
| 知识百科独立成节 | 通过（本次新增 `## 知识百科（3 篇）`，含总览 + 各篇标题与副标题） |
| 条目数 | 通过（23 条，规范建议 10–30） |
| 条目格式 | 通过（全部为 `- [标题](绝对地址)：描述`，无相对地址、无缺描述） |
| 联系方式小节 | 通过（含全国热线、客服手机/微信、服务时间、地址） |
| 文章收录 | 条件生效（`/articles/` 板块在有已发布文章后自动出现，当前文章数为 0） |

结论：结构与规范一致，可直接作为 AI 抓取入口。**迁移前后差异**：旧版为纯静态文案；
新版为数据驱动，条目数、课程数、问答数随后台内容变化，并新增文章收录与独立知识百科小节。

## 二、结构化数据（geo-schema）

产物：42 个 HTML 文件中 0 个 Microdata、0 个 JSON-LD 解析错误、无重复 `@id`（本次修复）。

| 节点 | 数量 | 状态 |
| --- | --- | --- |
| EducationalOrganization（机构，`@id .../#organization`） | 25 | 通过（name/url/logo/description/foundingDate/telephone/address/numberOfEmployees/knowsAbout 齐全） |
| EducationalOrganization + LocalBusiness（13 个校区） | 325 | 通过（含 telephone、PostalAddress、areaServed、parentOrganization、交通指引） |
| WebSite | 25 | 部分（无 `potentialAction`：站点无站内搜索，不虚构 SearchAction） |
| BreadcrumbList | 23 | 通过 |
| Course / CourseInstance / Offer | 16 / 16 / 16 | 通过 |
| FAQPage + Question/Answer | 9 / 52 | 通过 |
| Article + SpeakableSpecification | 3 | 通过（知识百科长文；文章详情页同模板） |
| AboutPage / ContactPage / CollectionPage | 3 / 1 / 3 | 通过（本次补 `url`、去掉与 WebPage 重复的 `@id`） |

本次修复项：

1. `/brand/`、`/contact/`、`/knowledge/`、`/students/` 原先同一 `@id`（`#webpage`）上有两个节点
   （WebPage + AboutPage/ContactPage/CollectionPage），已合并为单一最具体类型节点，并保留
   `isPartOf`、`datePublished/dateModified`、`url`；
2. 新增站点设置字段**官方主页链接（sameAs）**，构建时自动合并进机构节点
   （`src/components/Schema.astro`），运营填了即刻生效，无需改代码。

评测结论（按技能评分表）：**约 85/100**。扣分集中在 `sameAs` 为空（该项占 15 分）与
WebSite 的 `potentialAction`（占 5 分，本站无搜索，属不适用）。

**待运营补齐（无需开发）**：到后台「站点设置 → 官方主页链接」填入真实可访问的主页
（微信公众号文章页 / 抖音 / 视频号 / B 站 / 小红书等，越多越好），这是 AI 判定
「官网与各平台是同一实体」权重最高的信号。

## 三、AI 可引用性（geo-citability）

评分对象说明：AI 生成文章需配置 `AI_API_KEY` 后才会产出，当前库中文章数为 0；
因此以**结构最接近的已有长文** `/knowledge/guide-geo/`（1186 字，6 个内容块）作为样本评估，
结论可直接用于校正 AI 稿件的 Prompt 与校验阈值。

| 维度 | 权重 | 得分 | 说明 |
| --- | --- | --- | --- |
| 答案块质量 | 30% | 65 | 5 个 H2 中 2 个以定义/答案开篇（「GEO（生成式引擎优化）是指…」），其余以叙述开篇 |
| 段落自足性 | 25% | 72 | 各块点明了主题，但导语段混入日期/阅读时长等页面元信息，单独摘出会丢失语境 |
| 结构可读性 | 20% | 80 | H1→H2 层级干净、标题含问句、有 1 个对比表格与 2 个列表；无 H3 细分 |
| 数据密度 | 15% | 45 | 约 0.4 个数据点/500 字，远低于规范建议的 5+；无具名来源与年份 |
| 独特性 | 10% | 60 | 有官方教研视角与内部观察，但无自有一手数据或调研 |
| **合计** | | **66/100** | 高于 70 的内容块占比 33% |

改写建议（已对照文章页模板，确认新建文章页已覆盖下列短板）：

1. **每个 H2 都以 40–120 字的完整答案开篇**，再展开论证 —— 文章页的「答案段落」区块与
   `validate.ts` 的答案长度提醒项已按此设计，保持不放松即可；
2. **正文每 500 字至少 2 个带年份/来源的数据点** —— 文章页用「关键数据」区块兜底，
   但正文同样要落数字；建议运营优先补「知识条目 → 数据点」字段，模型会直接引用；
3. **导语段不要混入页面元信息**（日期、阅读时长），首段就点明主体；
4. **文末给出引用来源清单** —— 文章页已有 `sources` 区块，知识百科建议在后台正文中补一段
   「参考来源」，与文章保持同一规范。

## 四、未执行项与原因

| 项目 | 状态 | 原因与后续动作 |
| --- | --- | --- |
| geo-content（E-E-A-T 内容质量） | 未执行 | 评估对象是 AI 生成文章，需先配置 `AI_API_KEY` 并用 `npm run cms:ai:run` 产出草稿；产出后对草稿 HTML 执行即可 |
| tabbit（浏览器逐页核对） | 未执行 | 需同时常驻官网与后台并人工逐页截图；本轮已用构建产物脚本核对页面结构、内容与联系方式，建议在正式上线前用该技能做一次人工确认 |
| AI 稿件硬校验实测 | 未执行 | 同上，缺模型密钥；校验器已用 `ai:preview` 验证输入侧（品牌事实 2 条、命中素材 6 条），输出侧待首篇稿件落地后验证 |

## 五、结论

- llms.txt 与结构化数据两项通过校验，且比迁移前更规范（数据驱动 + 修复重复 `@id` + 补齐 `url`）；
- 可引用性样本得分 66，短板集中在「数据密度」与「答案开篇」，新建文章页模板已针对性覆盖；
- 唯一需要在运营侧补齐的高价值项是机构 `sameAs`（官方主页链接），后台已提供字段。

另：本轮校验过程中发现并修复了一处构建隐患 —— 取数层的快照目录原先用 `import.meta.url` 定位，
而 `astro build` 会把该模块打进 `dist/.prerender/chunks`，导致「后台不可用 + 有快照」时构建失败。
修复后已在**关闭后台**的情况下完整构建通过（25 页、llms.txt 23 条、结构化数据齐全），
离线与 CI 构建能力得到实证。
