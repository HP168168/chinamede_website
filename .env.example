# ============================================================
# 官网（Astro 静态站）构建期配置
# 复制为 .env 后填入实际值；本文件不含任何密钥，可安全入库。
# ============================================================

# Strapi 后台地址。留空时官网降级读取 src/data/__snapshot__/*.json 快照（离线也能构建）
STRAPI_URL=http://localhost:1337

# 只读 API 令牌：由 `cd cms; npm run init:tokens` 生成，或在后台「设置 → API 令牌」创建
# 仅授予已发布内容的读取权限；本变量只在服务端构建期使用，不会打进前端产物
STRAPI_TOKEN=

# 站点基准地址覆盖项（可选）。
# 默认读取后台「站点设置 → 站点基准地址」，仅在需要临时切换到预览域名时使用
# SITE_URL=https://www.chinamede.com
