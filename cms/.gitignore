# 依赖与构建产物
node_modules/
dist/
build/
.cache/
.tmp/

# 内容导出备份（体积大，按需单独归档）
backup/

# 环境变量（真实密钥不入库，仅提交 .env.example）
.env
.env.local
.env.*.local

# 上传的媒体文件（体积大，单独备份；保留目录占位文件）
public/uploads/*
!public/uploads/.gitkeep

# 日志
*.log
*.err
npm-debug.log*
